# Game Collection Tracker API (MVP)

## Overview
Simple RESTful API to manage a personal game collection. Built with Node.js, Express, Sequelize, and SQLite. This MVP includes 3 resources (Users, Games, Reviews), full CRUD, database seeding, basic middleware, error handling, and initial tests.

## Setup

1. Clone repository
2. Install dependencies
```bash
npm install
